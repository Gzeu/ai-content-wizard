{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "ai-content-wizard",
  "version": "1.0.0",
  "description": "A powerful AI-powered content generation tool using Groq's API",
  "main": "src/ai-provider.js",
  "bin": {
    "ai-wizard": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "dev": "node --watch src/cli.js",
    "test": "cross-env NODE_ENV=test mocha --require test/setup.js test/**/*.test.js --timeout 10000",
    "test:watch": "cross-env NODE_ENV=test mocha --require test/setup.js --watch test/**/*.test.js",
    "test:debug": "cross-env NODE_ENV=test DEBUG_GROQ=true node test-app.js",
    "test:coverage": "cross-env NODE_ENV=test nyc --reporter=text mocha --require test/setup.js test/**/*.test.js",
    "coverage": "nyc --reporter=lcov --reporter=text mocha test/**/*.test.js",
    "coverage:report": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "lint": "eslint src/**.js",
    "format": "prettier --write \"**/*.{js,json,md}\"",
    "build": "pkg . --out-path dist"
  },
  "nyc": {
    "all": true,
    "include": [
      "src/**/*.js"
    ],
    "exclude": [
      "**/*.test.js",
      "**/test/**",
      "**/node_modules/**"
    ],
    "reporter": [
      "text",
      "lcov",
      "clover"
    ],
    "check-coverage": true,
    "branches": 80,
    "lines": 80,
    "functions": 80,
    "statements": 80
  },
  "keywords": [
    "ai",
    "groq",
    "llm",
    "content-generation",
    "cli",
    "llama3",
    "mixtral",
    "ai-tools"
  ],
  "author": "Gzeu <gzeu@example.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Gzeu/ai-content-wizard.git"
  },
  "bugs": {
    "url": "https://github.com/Gzeu/ai-content-wizard/issues"
  },
  "homepage": "https://github.com/Gzeu/ai-content-wizard#readme",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^11.0.0",
    "dotenv": "^16.3.0",
    "fs-extra": "^11.2.0",
    "inquirer": "^9.2.0",
    "ora": "^7.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "codecov": "^3.8.2",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0",
    "pkg": "^5.8.0",
    "prettier": "^3.0.0",
    "sinon": "^15.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pkg": {
    "outputPath": "dist",
    "targets": [
      "node18-linux-x64",
      "node18-macos-x64",
      "node18-win-x64"
    ],
    "assets": [
      ".env"
    ]
  }
}
